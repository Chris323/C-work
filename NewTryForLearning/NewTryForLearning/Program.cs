using System;

namespace NewTryForLearning
{
    //Name does NOT have to match file, class name can be different
    class Program
    {
        //C# has many main signatures, not just the one like java as seen below
        //public static main (String[] args){} 
        static void Main(string[] args)
        {
            Console.WriteLine("Hello there!");
        }
    }
}
